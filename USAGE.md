# LÉTRA 카드뉴스봇 사용 설명서

뉴스 발췌문을 넣으면 카드뉴스 이미지를 자동으로 만들어 슬랙에 올려주는 도구예요.
폰만 있으면 되고, 터미널이나 컴퓨터는 필요 없어요.

---

## 1. 매주 이렇게 하면 돼요 (카드뉴스 만들기)

1. **GitHub 앱**(또는 브라우저에서 github.com) 열기
2. `card-news-bot` 저장소로 이동 → **Actions** 탭
3. 왼쪽 목록에서 **"Card News"** 선택
4. **Run workflow** 버튼 탭
5. `excerpt` 칸에 오늘 쓸 뉴스 발췌문(또는 소재)을 입력
6. **Run workflow** 버튼으로 실행
7. 1~2분 기다리면 슬랙 `#sns` 채널에 카드뉴스 이미지 7~10장 + 캡션이 도착해요

> 영어·스페인어 뉴스도 그대로 붙여넣으면 돼요. 자동으로 언어를 인식해서 2번 슬라이드에 원문 한 문장과 한국어 번역을 넣어줘요.

## 2. 쓰레드 문구 만들기

같은 방법으로 **"Threads Post"** 워크플로우를 실행하면 돼요. `topic` 칸에 주제를 적으면 150자 내외 문구 3개가 슬랙으로 와요.

---

## 3. 올리기 전에 꼭 확인할 것

- [ ] **발췌문이 직접 인용(따옴표)이 아니라 통계·사실 참고**로 되어 있는지 (저작권 문제 예방)
- [ ] **출처 표기**가 있는지 (매체명 · 필자 · 날짜)
- [ ] 원문 링크가 있다면 **Linktree(프로필 링크)에 이번 포스트 제목으로 등록**했는지
- [ ] 캡션 맨 아래 **해시태그**가 잘 붙어있는지 (`#필사 #필사스타그램 #뉴스필사 #오늘의문장 #자기계발 #letra #타이핑필사` + 주제 키워드)
- [ ] 이미지·캡션이 마음에 들면 인스타그램/쓰레드에 **직접 게시** (자동 게시는 안 함 — 사람이 항상 검토 후 올려요)
- [ ] 슬랙 캡션 맨 아래 **"댓글 추천:"** 문구 확인 — 게시 직후 첫 댓글로 그대로 달아주면 초반 참여 유도에 도움돼요

---

## 4. 브랜드 룰 요약

:::
계정명은 **LÉTRA** (E에 스페인어 강세, é). 빈티지 신문 무드지만 촌스럽지 않게, 정제된 에디토리얼 느낌을 유지해요.
:::

- **발행 주기**: 매주 월요일 1회 (제작자가 부업으로 운영 — 매일 발행 안 함)
- **폰트**: Hahmlet (구글 폰트, 옛 활판인쇄 느낌)
- **카드 하단**: 왼쪽엔 항상 "LÉTRA", 출처가 있는 슬라이드는 오른쪽에 "출처 · OO 참고"
- **고정 해시태그 7개**: `#필사` `#필사스타그램` `#뉴스필사` `#오늘의문장` `#자기계발` `#letra` `#타이핑필사`
- 이 규칙들은 전부 저장소의 `CLAUDE.md` 파일에 저장되어 있어서, 카드뉴스를 만들 때마다 자동으로 적용돼요

---

## 5. 카드뉴스 디자인 보관함

만들어진 카드뉴스와 텍스트, 해시태그는 Figma의 **"SNS"** 페이지에 정리해두고 있어요 (이미지, 슬라이드 카피, 캡션, 쓰레드 문구까지). 예전 포스트 확인하거나 디자인 참고할 때 거기서 보시면 돼요.

---

## 6. 문제가 생겼을 때

**Q. Actions 탭에서 "Run workflow" 버튼이 안 보여요**
→ 저장소 메인 화면이 아니라 **Actions 탭 → 워크플로우 이름 클릭**까지 들어가야 버튼이 나타나요.

**Q. 워크플로우가 빨간 X로 실패했어요**
→ 실패한 실행을 눌러서 어떤 단계에서 멈췄는지 확인하고, 에러 메시지를 그대로 복사해서 물어보면 원인을 찾아드려요.

**Q. 슬랙에 아무것도 안 왔어요**
→ 워크플로우가 초록색 체크로 끝났는지 먼저 확인하고, `#sns` 채널에 봇이 초대되어 있는지 확인해주세요.

**Q. 1년 후 갑자기 안 될 수도 있다던데?**
→ 인증 토큰이 1년 후 만료돼요. 그때 터미널에서 `claude setup-token`으로 새로 발급받아서 GitHub 저장소 Secrets의 `CLAUDE_CODE_OAUTH_TOKEN`을 교체하면 돼요 (미리 안내해드릴게요).

---

## 7. 다른 방법들 (참고용)

- **컴퓨터 + Claude Code에서 직접**: `/card-news 오늘자 OO신문 사설 중 "..." 발췌문`
- **컴퓨터를 켜둔 상태로 슬랙 멘션**: `npm run bot`으로 봇을 켜두면 슬랙에서 `@LÉTRA봇 카드뉴스 만들어줘: ...`로 바로 요청 가능
- 자세한 개발자용 설정은 저장소의 `README.md`, `deploy/README.md` 참고

---

## 참고 정보

- GitHub 저장소: `card-news-bot` (비공개 아님, lanis1001 계정)
- 카드뉴스 워크플로우: Actions → Card News
- 쓰레드 워크플로우: Actions → Threads Post
- 인증 방식: Claude 구독 재사용 (API 결제 없음), 1년마다 토큰 재발급 필요
